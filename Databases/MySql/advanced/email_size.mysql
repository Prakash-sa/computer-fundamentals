//https://app.codesignal.com/arcade/db/when-was-it-the-case/KbjWnZy3cpPJxj2st/solutions

SELECT id, email_title, 
        IF(size >= POW(2,20), 
           CONCAT(FLOOR(size/POW(2,20)), ' Mb'), 
           CONCAT(FLOOR(size/POW(2,10)), ' Kb')) AS short_size
    FROM emails
    ORDER BY size DESC;